DIFF_PARAMETERS = [
    '--histogram',          # Generate a diff using the "histogram diff" algorithm.
    # '--function-context',   # Show whole function as context lines for each change.
    '-U1000'                # Show the full file
]
